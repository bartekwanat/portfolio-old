import React from 'react';
import { StyledSectionTitle } from './AboutMeItem/AboutMeItem.styles';

export function SectionTitle(props) {
  return <StyledSectionTitle>{props.title}</StyledSectionTitle>;
}
